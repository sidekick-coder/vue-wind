export interface Props {
    defaultClass: string | string[];
    color: string;
    text: boolean;
    block: boolean;
    rounded: boolean;
    loading: boolean;
    spinSize: string;
    spinSeep: string;
}
